commit shedule is random because working on two different computers and need to sync changes
The other issue was that I was trying to get as much of it written before testing each little bit.
if no third serial on pico use sp2 for the sensor and rewrite adurino code for that. 